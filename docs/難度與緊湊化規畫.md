# 難度提升 × 畫面緊湊化 規畫文件

> 範圍限制：**美術風格與素材不調整**（沿用 BootScene 程序生成的圖）。
> 本次只動：陣型密度、數值平衡、波次節奏、敵人行為、魔王強化。
> 目標：擺脫「小朋友遊戲」的鬆散感 → 緊湊、有壓力、需要操作。

---

## 一、問題診斷（現況）

| 面向 | 現況 | 問題 |
|------|------|------|
| 士兵陣型 | 間距 22px、每列 5 人、最多 20 人 | 太鬆散、佔畫面、像玩具 |
| 敵人行為 | 一般敵兵**完全不會射擊**，只直線前進 | 沒有反擊壓力，純粹靠撞 |
| 波次節奏 | 波間隔 5–6 秒，常常空畫面 | 鬆、悶、無連續壓迫 |
| 數值 | 敵兵 HP 4 / 騎兵 12，傷害 1–2 | 偏軟，秒殺、無威脅 |
| 獎勵門 | 全是正向（+ / ×），無風險 | 無取捨、無懲罰、無策略 |
| 魔王 | HP 150、三相已做，但子彈慢（320） | 相位有了但壓力仍不足 |
| 畫面 | 800×480，中央大量留白 | 不緊湊 |

---

## 二、畫面緊湊化（佈局，**不碰美術**）

### 2.1 士兵陣型加密
`GameScene.updateSoldierPositions` / `firePlayerBullets`

| 參數 | 現值 | 建議 | 效果 |
|------|------|------|------|
| 列內 Y 間距 | 22px | **13px** | 同樣人數佔垂直空間少 ~40% |
| 欄間 X 間距 | 22px | **15px** | 縱深更密、像方陣 |
| 每欄人數 | 5 | **6** | 同寬度容納更多 |
| 最大可見數 | 20 | **30** | 滿員視覺更壯觀、更密 |
| bob 抖動振幅 | 2.5 | **1.5** | 減少鬆散晃動感 |

> 新增常數：`SOLDIER_GAP_Y = 13`、`SOLDIER_GAP_X = 15`、`SOLDIER_PER_COL = 6`、`SQUAD_MAX_VISUAL = 30`，取代散落在程式裡的魔術數字 22 / 5 / 20。

### 2.2 戰場縱向收緊
- `SQUAD_MIN_Y` 70 → **90**、`SQUAD_MAX_Y` 410 → **400**：把可活動帶略收，逼近戰，留白變少。
- 敵人生成 Y 範圍對齊上面收緊後的活動帶（目前 110 起算），讓子彈與敵人更常處於同一條走廊。

### 2.3 敵人陣型加密
`spawnWave`：欄間距 `col * 38` → **`col * 28`**，讓敵群更像「一堵牆」壓過來，而非稀疏點點。

---

## 三、難度提升

### 3.1 敵兵會射擊（最大改動，核心壓力來源）
目前只有魔王開火，這是「太簡單」的主因。

- 騎兵（cavalry）獲得低頻直線射擊：進場後每 `1600–2200ms` 朝左發 1 發（沿用 `enemy_bullet` 貼圖，**不需新美術**）。
- 後段波次的精英士兵（標記 `canShoot`）每 `2200ms` 發 1 發。
- 子彈速度沿用 `ENEMY_BULLET_SPEED`，但對一般敵新增較慢值 `ENEMY_BULLET_SPEED_GRUNT = 240`，與魔王彈拉開層次。
- 實作：`spawnEnemy` 接受 `fireRate?` 參數，敵人自帶 `time.addEvent`，敵人被清除時一併 `remove`。

### 3.2 數值重平衡（隨波次遞增）
新增 `waveScale(n)`：HP 與數量隨波次線性放大。

| 項目 | 現值 | 規畫 |
|------|------|------|
| 敵兵 HP | 4 | `4 + floor(wave/2)`（第10波 ≈ 9） |
| 騎兵 HP | 12 | `12 + wave`（第10波 ≈ 22） |
| 敵兵傷害 | 1 | 1（不變，靠數量壓） |
| 騎兵傷害 | 2 | **3** |
| 騎兵速度 | 140 | 140 → 後段波 **170** |

### 3.3 波次節奏收緊
- 波間隔 5–6s → **3–4s**，並讓相鄰波**重疊 spawn**（前一波尾段，下一波已開始進場），消除空畫面。
- 維持 **10 波**架構，但密度與並行度提高。
- 後段（7–10 波）採「士兵牆 + 騎兵突進」混編，逼玩家走位躲彈又要保持火力覆蓋。

### 3.4 獎勵門引入風險取捨
目前雙門都是正向，無腦選大的即可。改為：

- 部分關卡一門高收益、一門低收益（如 `×3` vs `+8`），數值上互有勝負，逼玩家估算當前人數。
- 引入**負面/陷阱門**（如 `-8`、`÷2`）與正向門配對，選錯就掉人 → 真正的決策點。
- 門移動速度 `GATE_SPEED` 200 → **240**，縮短猶豫時間。

### 3.5 魔王強化（沿用三相架構）
- HP 150 → **220**，硬度匹配滿編火力。
- 子彈速度：對魔王彈設 `BOSS_BULLET_SPEED = 380`（比雜兵彈快、有威脅）。
- Phase 1（>66%）：3 叉散射，**加一發直瞄**（4 發）。
- Phase 2（33–66%）：瞄準連射，由 3 發 → **5 發扇形**，射速 950 → **820ms**。
- Phase 3（<33%）：8 方向散射 → **12 方向**（每 30°），射速 550 → **480ms**，並每隔一輪夾帶一次直瞄齊射（彈幕節奏變化）。
- 進場時生成 2 隻護衛騎兵，邊打魔王邊清場。

---

## 四、難度曲線總表（10 波 + 魔王）

> ⚠️ **已由第九節取代**：分波結構已改為「連續無縫生成」，不再有 10 波與關卡標示。以下保留作為設計演進紀錄。
> 人數已依「軍團牆」密度上修（見第七節 mp4 參考）。

| 波 | 內容 | 是否射擊 | 重點壓力 |
|----|------|----------|----------|
| 1 | 16 士兵 | 否 | 教學手感 |
| 2 | 24 士兵 | 否 | 數量初壓 |
| — | 門：`+5` / `+3` | — | 暖身決策 |
| 3 | 6 騎兵 | **是** | 首次吃彈 |
| 4 | 28 士兵 | 否 | 軍團牆 |
| — | 門：`×2` / `-6`（風險） | — | 首個陷阱門 |
| 5 | 8 騎兵 | **是** | 彈幕走位 |
| 6 | 32 士兵 + 3 精英(射擊) | 部分 | 牆+冷箭 |
| — | 門：`+10` / `÷2`（風險） | — | 高張力決策 |
| 7 | 10 騎兵 | **是** | 密集彈 |
| 8 | 24 士兵 + 5 騎兵 混編 | 部分 | 兩線壓力 |
| — | 門：`×3` / `+8` | — | 估算取捨 |
| 9 | 8 騎兵 + 18 士兵 | **是** | 牆+彈幕 |
| 10 | 14 騎兵 + 20 士兵 | **是** | 最終衝鋒 |
| — | 門：`+20` / `×3` | — | 決戰補給 |
| BOSS | 三相魔王 + 2 護衛 | **是** | 彈幕總考 |

---

## 五、實作清單（待核准後執行）

1. **constants.ts**：新增陣型常數、敵彈速度分層、魔王數值、`waveScale`。
2. **GameScene — 佈局**：`updateSoldierPositions` / `firePlayerBullets` 改用新間距常數（同步調整子彈出口）。
3. **GameScene — 敵人射擊**：`spawnEnemy` 加 `fireRate`，自帶開火 timer，清除時 remove。
4. **GameScene — 波次**：`scheduleWaves` 收緊節奏、混編、加陷阱門。
5. **GameScene — 魔王**：三相彈量/彈速強化、護衛生成。
6. **GameScene — 門**：`applyGateEffect` 支援 `-` / `÷`，`GATE_SPEED` 提速。
7. 邊界保護：滿編 30 人時子彈/敵彈池容量複查（player 300、enemy 已調 150，混編波需驗證上限）。

> 美術完全沿用現有貼圖；精英兵、護衛騎兵、敵彈皆復用既有 texture，無新增繪圖。

---

## 六、方向定案（已確認 ✔）

- **獎勵門：引入陷阱門** ✔ — 負面門（`-6`、`÷2`）與正向門配對，建立真實決策點。實機若挫折感過重，再調整負面幅度。
- **敵兵射擊：騎兵 + 後段精英兵** ✔ — 全量落地，依第四節曲線表執行。
- **滿編士兵：30 人** ✔ — 採最緊湊方案；若實機過擠或可讀性下降，預留退回 24 的微調空間。

---

## 七、mp4 參考設計（軍團密度 / 彈幕 / 武器進程）

> ⚠️ **本節多項已由第九節調整**：武器改門控+直線、敵人改連續生成。7.1/7.2/7.3 的數值見第九節最新版，本節保留演進紀錄。

參考資料夾 `錄製內容…mp4`（已抽幀 `frames/`）。畫面特徵：**整片密集軍團如地毯鋪滿路面**、**部隊噴出又寬又密的子彈洪流**、隨進度**換槍、射速越來越快**。據此把「以一擋百」的爽快感落地，**美術仍沿用既有貼圖**（僅子彈底圖改白以利染色、門/曳光用 runtime tint）。

### 7.1 武器階級系統（逐步增加射速）— 核心新功能
五階武器，**每擊殺 `WEAPON_KILL_STEP = 12` 隻自動升一階**；射速遞增、彈幕加寬、曳光換色，頂階每兵雙發。

| 階級 | 射速(ms) | 每兵彈數 | 扇角(±°) | 抖動(±°) | 曳光色 |
|------|---------|---------|---------|---------|--------|
| PISTOL  | 200 | 1 |  8 | 2 | 黃 `0xffee33` |
| RIFLE   | 140 | 1 | 14 | 3 | 橙 `0xffaa22` |
| SMG     |  95 | 1 | 20 | 4 | 青 `0x44ddff` |
| HEAVY   |  60 | **2** | 28 | 5 | 紅 `0xff5544` |
| MINIGUN |  42 | **2** | 36 | 6 | 白熱 `0xffffff` |

- 升級回饋：畫面藍閃 + 中央「WEAPON UP! ◯◯」橫幅 + HUD 左上武器名跳動（`weaponChanged` 事件 → UIScene）。
- 設計意圖：**密度與火力同步成長** — 敵人越多、你的槍越猛，重現參考片的碾壓爽感。
- 子彈底圖（`bullet`）由黃改白，使 tint 染色準確；PISTOL 用黃 tint 維持原外觀。

### 7.2 敵軍密度（地毯牆）
- 陣型改為**固定列距 `ENEMY_ROW_GAP = 26px`、垂直置中的密集方塊**（取代原本「均分整個高度」的稀疏排法）→ 成排成列鋪滿，貼近影片地毯感。
- 進場欄間隔 360ms → **200ms**，連成綿密推進的牆；欄距 `ENEMY_FORM_GAP_X` → **22px**。
- 波次人數/列數大幅上修（見第四節表）：W4 36/6列、W6 42/6列+精英、W10 騎兵16+士兵28。
- 後段波次推「**士兵牆 + 騎兵彈幕**」雙線（W8/9/10），地毯式壓迫。

### 7.3 子彈彈幕：發散扇形噴泉（取代原收束做法）
> 修正：先前用「收束到一點」導致子彈仍像直線。改為**向外發散**。

- 全隊子彈攤開在 **±`spreadDeg` 扇形錐**內（PISTOL ±8° → MINIGUN ±36°），槍口起點再沿垂直帶 `MUZZLE_BAND = 64px` 鋪開 → 整片噴泉覆蓋走廊。
- 每發加 `jitterDeg` 隨機散射 → 噴射手感而非齊射線；子彈 sprite 依飛行方向旋轉。
- HEAVY / MINIGUN 每兵雙發；玩家子彈池 300 → **600**；玩家彈上下出界一併回收。

### 7.4 屠殺連擊回饋（爽感）
- **Combo**：`700ms` 內連續擊殺累加 → HUD 右側 `COMBO ×N`，數字越高顏色越燙（黃→橙→紅）。
- 爆裂粒子量隨 combo 放大（14 → 26）；combo ≥3 觸發微震動，強度隨連擊上升。
- 爆炸音**節流**（每 3 殺一次），密集波不糊成噪音。

### 7.5 待實機微調旋鈕
- `WEAPON_KILL_STEP`（升級節奏，現 12）：太快→調大、想更早爽→調小。
- `spreadDeg`：扇形太散打不中 → 調小。
- 敵軍量 / 列數：手機卡頓 → 降人數或列數。
- combo 視窗（現 700ms）、`bullet` 池 `maxSize`（MINIGUN 變稀 → 上調）。

---

## 八、實作狀態

第一～三節（緊湊化 + 難度系統）、第六節定案、第七節（mp4 參考）、**第九節（連續生成重構）皆已實作並通過 `tsc` 與 `vite build`**。後續為實機體感微調，非新功能。

---

## 九、連續生成重構（依實機回饋，✔ 已實作 — 最新基準）

實機回饋四點，據此把核心循環從「分波」改為「連續軍團 + 門控成長」：

### 9.1 子彈：純左到右直線（取代 7.3 發散扇形）
- 每兵從自身槽位**水平射出** `velocity (BULLET_SPEED, 0)`，不再扇形/收束/抖動。
- 高階武器為「同位置疊射多發」（`BULLET_STACK_GAP = 5px`），仍全直線。

### 9.2 武器：獎勵門解鎖（取代 7.1 擊殺自動升級）
- **移除擊殺自動升級**（原 `WEAPON_KILL_STEP`，是「秒衝第5階、射速失控」的根因）。
- 新增 **⚡ 武器門**（青色 tint）：吃到才 `weaponTier + 1`；滿階再吃 → 轉 +12 兵。
- 射速階梯收斂，最快也遠慢於舊版：

| 階級 | 射速(ms) | 每兵彈數 | 曳光色 |
|------|---------|---------|--------|
| RIFLE   | 180 | 1 | 黃 |
| BURST   | 150 | 1 | 橙 |
| RAPID   | 115 | 2 | 青 |
| HEAVY   |  92 | 2 | 紅 |
| MINIGUN |  74 | 2 | 白熱 |

- 獎勵門輪播池（火力 vs 數量取捨）：`⚡/+25`、`×2/-10`、`⚡/×2`、`+30/÷2`、`⚡/+20`、`×2/+18`，每 9s 一組。

### 9.3 敵人：連續軍團、密度 ×10（取代 7.2 分波牆）
- **連續生成器**：每 `HORDE_INTERVAL = 380ms` 噴出一整條滿屏高度密集縱列（`HORDE_Y_TOP 64`→`HORDE_Y_BOT 424`，列距 `HORDE_ROW_GAP = 18px`，約 21 兵/列）。
- **基本士兵永不停**；`ENEMY_CAP = 240` 為同屏效能護欄（約舊版 10 倍密度）。
- 週期性**騎兵射擊小隊**（每 6.5s）加彈幕；HP/射速隨遊戲時間遞增（`soldierHp/cavalryHp/elapsed`）。

### 9.4 無縫接軌（取代第四節分波 + 關卡標示）
- 刪除 `WAVE X/10` 提示與整個分波排程 → 連續無縫。
- 魔王於 `BOSS_AT_MS = 78s` 登場（軍團轉稀 ×2.4 間隔但不停、暫停門與騎兵）；擊殺魔王 = 勝利並停止所有生成器。

### 9.5 待實機微調旋鈕（最新）
- `HORDE_INTERVAL` 380（更密→調小）、`ENEMY_CAP` 240（卡頓→調小）。
- `BOSS_AT_MS` 78000（太久→調小）。
- 武器門頻率（gate timer 9s）、`bullet` 池 `maxSize 600`（MINIGUN 變稀→上調）。
- 連擊視窗 700ms。

> 設計循環：直線火力清自身所在「車道」→ 吃 ⚡ 升火力 / 吃數量門擴編 → 撐到 78s 魔王 → 擊殺即勝。敵牆多數從畫面外側流過（視覺密度），威脅集中在車道內敵兵與騎兵彈幕。
